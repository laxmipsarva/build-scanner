name: Test action

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run build-scanner against test fixtures
        uses: ./
        with:
          path: tests/fixtures
          format: json
